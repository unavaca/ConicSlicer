package com.slicing;

import java.io.File;
import java.io.IOException;

import com.model.Settings;

/**
 * Combines lower (conic) + upper (planar) g code into one file.
 * 
 * @author Zach Brinton
 * @version 3-6-26
 */
public class GCodeMerger {
	public static void merge(File lowerGcode, File upperGcode, Settings s, File outGcode) throws IOException {
		// TODO
	}
}
